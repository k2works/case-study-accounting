<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.accounting.infrastructure.persistence.mapper.AutoJournalLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.example.accounting.infrastructure.persistence.entity.AutoJournalLogEntity">
        INSERT INTO auto_journal_logs (pattern_id, executed_at, processed_count, generated_count, status, message,
        error_detail)
        VALUES (#{patternId}, #{executedAt}, #{processedCount}, #{generatedCount}, #{status}, #{message},
        #{errorDetail})
    </insert>

</mapper>
